# homebrew-keystats

[keystats](https://github.com/gapul/keystats) の Homebrew tap。

```sh
brew install gapul/keystats/keystats
```

インストール後、Launchpadなどから `Keystats` を起動し、初回画面の案内に従ってください。
入力監視の許可は自動検知され、認識されない場合は画面内から修復できます。
